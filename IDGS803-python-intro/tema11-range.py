numeros = range(5) # Genera una lista de numeros de acuerdo al rango proporcionado 

numeros = range(2, 8) # Genera una lista de numeros iniciando desde el numero que esta al inicio

numeros = range(0, 10, 2) # Genera una lista de numeros de dos en dos

